(()=>{const q=(s,r=document)=>r.querySelector(s),qq=(s,r=document)=>[...r.querySelectorAll(s)],e=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),safe=v=>{try{const u=new URL(String(v||''),location.origin);return ['http:','https:'].includes(u.protocol)?u.href:'#'}catch{return'#'}};const search=q('[data-arts-search]'),grid=q('[data-arts-grid]');if(search&&grid){const cards=qq('[data-arts-neighborhood]',grid),count=q('[data-arts-count]'),apply=()=>{const term=search.value.trim().toLowerCase();let n=0;cards.forEach(card=>{const show=!term||(card.dataset.search||'').includes(term);card.hidden=!show;if(show)n++});if(count)count.textContent=`${n} neighborhood${n===1?'':'s'}`};search.addEventListener('input',apply);apply()}const card=x=>`<article class="neighborhood-exhibit-card"><span>${e(x.status||'On view')}</span><h3><a href="${safe(x.url)}" target="_blank" rel="noopener noreferrer">${e(x.title)}</a></h3><div class="museum">${e(x.museum||'Museum exhibition')}</div><div class="dates">${e(x.start_date||'')}${x.end_date&&x.end_date!==x.start_date?` – ${e(x.end_date)}`:''}</div>${x.summary?`<p>${e(x.summary)}</p>`:''}</article>`;async function neighborhood(){const match=location.pathname.match(/^\/neighborhood\/([^/]+)/);if(!match||q('.neighborhood-arts'))return;const response=await fetch('/api/bulletin',{headers:{Accept:'application/json'}});if(!response.ok)return;const data=await response.json(),edition=data?.editions?.[match[1]],arts=data?.arts;if(!edition||!arts?.configured)return;const desk=arts.neighborhoods?.[edition.name],exhibits=desk?.exhibitions||[];if(!exhibits.length)return;const section=document.createElement('section');section.className='neighborhood-arts';section.id='arts';section.innerHTML=`<div class="neighborhood-arts-head"><div><p class="section-label">ARTS</p><h2>On view in ${e(edition.name)}</h2></div><a class="action-link" href="/arts">Citywide arts →</a></div><div class="neighborhood-exhibit-grid">${exhibits.slice(0,4).map(card).join('')}</div><p class="arts-source-note">Exhibition dates come from official museum and presenting-institution schedules and refresh with the Bulletin. Check the linked museum before visiting because schedules can change.</p>`;const anchor=q('.neighborhood-real-estate')||q('.why-section')||q('.quick-read')||q('.neighborhood-front');anchor?.insertAdjacentElement('afterend',section)}neighborhood().catch(()=>{})})();